/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['visitswatvalley.com'], // Replace with the domain name, e.g., 'visitswatvalley.com'
    },
  };

  module.exports = nextConfig;
